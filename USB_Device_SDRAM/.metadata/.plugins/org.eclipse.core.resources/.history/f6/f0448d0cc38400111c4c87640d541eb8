#include "LPC24xx.h"
#include "usbhw.h"

void delay_ms(uint32_t ms) {
    for (volatile uint32_t i = 0; i < (ms * 10000); i++);
}

int main(void) {
    delay_ms(100);

    USB_Init();
    USB_Connect(true);

    while (1) {
    }
}
